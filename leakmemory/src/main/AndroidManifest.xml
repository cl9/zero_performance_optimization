<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zero.leakmemory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".set.SetActivity1" />
        <activity android:name=".set.SetActivity2" />
        <activity android:name=".set.SetActivity3" />
        <activity android:name=".set.SetActivity4" />
        <activity android:name=".set.SetActivity5" />
        <activity android:name=".singleton.SingletonActivity" />
        <activity android:name=".handler.HandlerActivity" />
        <activity android:name=".handler.SolutionHandlerActivity"/>
    </application>
</manifest>
